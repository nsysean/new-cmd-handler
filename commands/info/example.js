

module.exports = {
    name : 'example',
		aliases: ['e'],//optional u can delete this line
    description : 'description',

   execute : async(client, message, args) => {
/*code here for eg message.channel.send
args is message.content but without prefix + cmd
for message reply, do message.inlineReply(stuff)
*/

 
    }
}
